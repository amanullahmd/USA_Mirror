# Environment Variables for The USA Mirror
# Copy this file to .env and fill in your values

# Database Connection
# Replace with your VPS PostgreSQL connection string
DATABASE_URL=postgresql://username:password@localhost:5432/usa_mirror_db

# Session Secret (Generate a random string - keep this secret!)
# You can generate one at: https://randomkeygen.com/
SESSION_SECRET=your-very-long-random-secret-key-here-change-this

# Application Port (default: 5000)
PORT=5000

# Environment (production or development)
NODE_ENV=production

# OPTIONAL: PostgreSQL Connection Details (if not using DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=your_db_username
PGPASSWORD=your_db_password
PGDATABASE=usa_mirror_db

# INSTRUCTIONS:
# 1. Copy this file and rename it to .env
# 2. Replace all values with your actual VPS details
# 3. Generate a strong random string for SESSION_SECRET
# 4. Never commit the .env file to Git (it contains secrets!)
